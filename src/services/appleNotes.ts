// Apple Notes integration — Coming Soon
// Will use native APIs to sync notes for AI context

export const connectAppleNotes = async (): Promise<void> => {
  throw new Error('Apple Notes integration coming soon');
};

export const isAppleNotesConnected = async (): Promise<boolean> => {
  return false;
};

export const disconnectAppleNotes = async (): Promise<void> => {
  // No-op
};
